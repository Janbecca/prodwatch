# 作用：LLM：任务配置示例文件（用于本地/自测）。

# Example: per-task provider/model mapping.
llm_tasks:
  crawler_generation:
    provider: mock
    model: mock-v1
  post_analysis:
    provider: qwen
    model: qwen-plus
  keyword_extraction:
    provider: qwen
    model: qwen-plus
  report_generation:
    provider: deepseek
    model: deepseek-chat
